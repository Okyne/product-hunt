export const commentsCount = (state) => {
  return state.commentsCount
}

export const makersCount = (state) => {
  return state.makersCount
}

export const posts = (state) => {
  return state.posts
}

export const votesCount = (state) => {
  return state.votesCount
}
